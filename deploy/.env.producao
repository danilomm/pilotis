# Configuracoes de Producao - Pilotis
# Copie este arquivo para .env no servidor

# === BANCO DE DADOS ===
# IMPORTANTE: Caminho FORA do diretorio web para seguranca
DATABASE_PATH=/home/app/dados_privados/pilotis.db

# === PAGBANK ===
# Token de producao (obtido no painel PagBank)
PAGBANK_TOKEN=seu_token_de_producao_aqui
PAGBANK_SANDBOX=false

# === EMAIL (BREVO) ===
BREVO_API_KEY=sua_chave_brevo_aqui
EMAIL_FROM=Docomomo Brasil <tesouraria@docomomobrasil.com>

# === APLICACAO ===
# URL publica onde a aplicacao esta hospedada
BASE_URL=https://pilotis.docomomobrasil.com

# Chave secreta para sessoes (gere uma nova!)
# python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=gere_uma_chave_secreta_aqui

# Senha do painel admin (use hash em producao!)
# python -c "import hashlib; print('sha256:' + hashlib.sha256(b'sua_senha').hexdigest())"
ADMIN_PASSWORD=sha256:coloque_o_hash_aqui

# === VALORES (em centavos) ===
VALOR_ESTUDANTE=11500
VALOR_PROFISSIONAL=23000
VALOR_INTERNACIONAL=46000
